/**
 * Lecture: let, const and var
 * 1. Set the value of  'language' to the language spoken where you live (some countries have multiple language, but just choose one).
 * 2. Think about which variables should be const variables (which values will
 * naver change, and which might change?). Then change these variabels to const
 * 3.Try to change one of the change varibles now, and observe what happens
 */
// c country, Continent, population;
const country = "Bangladesh";
const Continent = "Asia";
let population = 164.7;
// 
const isIsland = true;
const language = "bangli";
console.log(`Country name ${country} in ${Continent} and pupulation ${population} million \n isIsland = ${isIsland},language is ${language}`);
/**
 * Country name Bangladesh in Asia and pupulation 164.7 million 
 isIsland = true,language is bangli
 */